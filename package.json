{
  "scripts": {
    "dead-code": "fallow dead-code && fallow dead-code --production",
    "fmt": "pnpm -r fmt",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "fallow": "^3.23.0"
  },
  "dependencies": {
    "vercel": "^59.11.7"
  }
}
